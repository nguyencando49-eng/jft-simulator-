import { NextResponse } from 'next/server';import { requireAuth } from '@/lib/server/auth';import { apiError } from '@/lib/server/http';import { getRepository } from '@/lib/server/repository';import { parseSource } from '@/lib/server/source-service';
export async function GET(req:Request){try{await requireAuth(req,'admin');return NextResponse.json({ok:true,sources:await getRepository().listSourceDocuments()});}catch(e){return apiError(e);}}
export async function POST(req:Request){try{const user=await requireAuth(req,'admin'),body=await req.json();if(typeof body.content!=='string')throw new Error('content is required');const source=parseSource(body,user.userId);await getRepository().saveSourceDocument(source);return NextResponse.json({ok:true,source},{status:201});}catch(e){return apiError(e);}}
