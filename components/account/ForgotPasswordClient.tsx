'use client';
import Link from 'next/link'; import { FormEvent,useState } from 'react'; import { authApi } from '@/lib/api-client';
export default function ForgotPasswordClient(){const [email,setEmail]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{await authApi.recover(email);setDone(true)}catch(e){setError(e instanceof Error?e.message:'Request failed')}finally{setLoading(false)}}return <main className="shell auth-page"><div className="auth-wrap"><section className="card auth-card"><div className="eyebrow">ACCOUNT RECOVERY</div><h1>Forgot password</h1><p>Nhập email. Nếu tài khoản tồn tại, hệ thống sẽ gửi link đặt lại mật khẩu.</p>{error&&<div className="admin-alert error">{error}</div>}{done?<div className="admin-alert ok">Đã gửi yêu cầu. Hãy kiểm tra email và thư mục spam.</div>:<form onSubmit={submit} className="auth-form"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="primary" disabled={loading}>{loading?'Sending…':'Send reset link'}</button></form>}<div className="auth-links"><Link href="/login">Back to sign in</Link></div></section></div></main>}
