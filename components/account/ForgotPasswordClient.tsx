'use client';
import Link from 'next/link'; import { FormEvent,useState } from 'react'; import { authApi } from '@/lib/api-client'; import { Alert } from '@/components/ui';
export default function ForgotPasswordClient(){const [email,setEmail]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{await authApi.recover(email);setDone(true)}catch{setError('Không thể gửi yêu cầu. Vui lòng thử lại sau.')}finally{setLoading(false)}}return <main className="auth-page auth-rebuild"><Link href="/" className="candidate-brand auth-brand"><span>日本語</span><b>JFT Practice</b></Link><section className="auth-card ui-card"><span className="eyebrow">ACCOUNT RECOVERY</span><h1>Quên mật khẩu</h1><p>Nhập email của bạn để nhận liên kết đặt lại mật khẩu.</p>{error&&<Alert tone="danger">{error}</Alert>}{done?<Alert tone="success">Nếu tài khoản tồn tại, chúng tôi đã gửi hướng dẫn đến email của bạn. Hãy kiểm tra cả thư mục spam.</Alert>:<form onSubmit={submit} className="auth-form"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" required/></label><button className="primary" disabled={loading}>{loading?'Đang gửi…':'Gửi liên kết đặt lại'}</button></form>}<div className="auth-links"><Link href="/login">← Quay lại đăng nhập</Link></div></section></main>}
