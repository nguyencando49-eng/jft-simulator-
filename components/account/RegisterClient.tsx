'use client';
import Link from 'next/link'; import { FormEvent,useState } from 'react'; import { useRouter } from 'next/navigation'; import { authApi } from '@/lib/api-client';
export default function RegisterClient(){const router=useRouter();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[displayName,setDisplayName]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await authApi.signup(email,password,displayName);if(r.verificationRequired)setDone('Đã tạo tài khoản. Hãy kiểm tra email để xác minh trước khi đăng nhập.');else router.replace('/candidate')}catch(e){setError(e instanceof Error?e.message:'Sign up failed')}finally{setLoading(false)}}return <main className="shell auth-page"><div className="auth-wrap"><section className="card auth-card"><div className="eyebrow">JFT SIMULATOR V5.1.2</div><h1>Create account</h1><p>Tạo tài khoản Candidate để lưu lịch sử thi và resume session trên nhiều thiết bị.</p>{error&&<div className="admin-alert error">{error}</div>}{done?<><div className="admin-alert ok">{done}</div><Link href="/login"><button className="primary">Go to sign in</button></Link></>:<form onSubmit={submit} className="auth-form"><label>Display name<input value={displayName} onChange={e=>setDisplayName(e.target.value)} maxLength={80}/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/></label><button className="primary" disabled={loading}>{loading?'Creating…':'Create candidate account'}</button></form>}<div className="auth-links"><Link href="/login">Already have an account? Sign in</Link></div></section></div></main>}
