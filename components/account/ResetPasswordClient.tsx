'use client';
import Link from 'next/link'; import { FormEvent,useEffect,useState } from 'react'; import { authApi } from '@/lib/api-client'; import { Alert } from '@/components/ui';
export default function ResetPasswordClient(){const [token,setToken]=useState(''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[done,setDone]=useState(false),[loading,setLoading]=useState(false);useEffect(()=>{const h=new URLSearchParams(location.hash.replace(/^#/,''));setToken(h.get('access_token')||new URLSearchParams(location.search).get('access_token')||'')},[]);async function submit(e:FormEvent){e.preventDefault();setError('');if(password!==confirm){setError('Hai mật khẩu chưa khớp.');return}try{setLoading(true);await authApi.resetPassword(token,password);setDone(true)}catch{setError('Liên kết không hợp lệ hoặc đã hết hạn.')}finally{setLoading(false)}}return <main className="auth-page auth-rebuild"><Link href="/" className="candidate-brand auth-brand"><span>日本語</span><b>JFT Practice</b></Link><section className="auth-card ui-card"><span className="eyebrow">ACCOUNT RECOVERY</span><h1>Đặt mật khẩu mới</h1>{error&&<Alert tone="danger">{error}</Alert>}{done?<><Alert tone="success">Mật khẩu đã được cập nhật.</Alert><Link href="/login" className="primary">Đăng nhập</Link></>:<form onSubmit={submit} className="auth-form"><label>Mật khẩu mới<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} autoComplete="new-password" required/></label><label>Nhập lại mật khẩu<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} minLength={8} autoComplete="new-password" required/></label><button className="primary" disabled={loading}>{loading?'Đang cập nhật…':'Cập nhật mật khẩu'}</button></form>}</section></main>}
