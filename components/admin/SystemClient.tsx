'use client';
import { useEffect,useState } from 'react';
import { adminApi } from '@/lib/api-client';
export default function SystemClient(){const [data,setData]=useState<{repository:string;authentication:string;assetStorage:string;aiFactory:string;apiVersion:string}|null>(null),[error,setError]=useState('');useEffect(()=>{void adminApi.system().then(r=>setData(r)).catch(e=>setError(e instanceof Error?e.message:'Load failed'))},[]);const rows=data?[['Repository',data.repository],['Authentication',data.authentication],['Asset storage',data.assetStorage],['AI factory',data.aiFactory],['API version',data.apiVersion]]:[];return <><div className="admin-title"><div><span className="eyebrow">V5 BACKEND</span><h1>System</h1><p>Runtime status, persistence, authentication and AI factory provider.</p></div></div>{error&&<div className="admin-alert error">{error}</div>}<section className="admin-panel"><div className="panel-head"><h2>Runtime</h2><span className="badge green">V5</span></div><div className="system-grid">{rows.length?rows.map(([k,v])=><div key={k}><span>{k}</span><b>{v}</b></div>):<p>Loading…</p>}</div></section><section className="admin-panel"><div className="panel-head"><h2>V5 API surface</h2></div><div className="endpoint-list"><code>POST /api/v1/auth/login</code><code>GET /api/v1/auth/me</code><code>POST /api/v1/auth/logout</code><code>POST /api/v1/auth/signup</code><code>POST /api/v1/auth/recover</code><code>POST /api/v1/auth/reset</code><code>GET/PATCH /api/v1/profile</code><code>GET/POST /api/v1/questions</code><code>GET/POST /api/v1/factory/jobs</code><code>GET /api/v1/factory/jobs/:id</code><code>POST /api/v1/factory/jobs/:id/approve</code><code>GET/PUT/POST /api/v1/exams</code><code>POST /api/v1/sessions</code><code>POST /api/v1/sessions/:id/submit</code><code>GET /api/v1/admin/candidates</code></div></section></>}
