'use client';
import { ReactNode,useEffect,useState } from 'react'; import { usePathname,useRouter } from 'next/navigation'; import { authApi,UserProfile } from '@/lib/api-client'; import type { UserRole } from '@/lib/server/domain';
export default function AuthGate({role,children}:{role?:UserRole;children:ReactNode}){const router=useRouter(),path=usePathname();const [user,setUser]=useState<UserProfile|null>(null),[state,setState]=useState<'loading'|'ok'|'forbidden'>('loading');useEffect(()=>{void authApi.me().then(r=>{setUser(r.user);if(role&&r.user.role!==role)setState('forbidden');else setState('ok')}).catch(()=>router.replace(`/login?next=${encodeURIComponent(path)}`))},[path,role,router]);if(state==='loading')return <main className="shell"><div className="page"><section className="card auth-card"><p>Checking session…</p></section></div></main>;if(state==='forbidden')return <main className="shell"><div className="page"><section className="card auth-card"><h1>Access denied</h1><p>Tài khoản hiện tại là <b>{user?.role}</b>, nhưng trang này yêu cầu quyền <b>{role}</b>.</p><button className="primary" onClick={async()=>{await authApi.logout();router.replace(`/login?next=${encodeURIComponent(path)}`)}}>Switch account</button></section></div></main>;return <>{children}</>}
