'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation'; import UserMenu from '@/components/auth/UserMenu';
export default function CandidateShell({children}:{children:React.ReactNode}){const path=usePathname();const links=[['/candidate','Trang chủ'],['/candidate#exams','Bài luyện tập'],['/candidate#history','Lịch sử'],['/candidate/profile','Hồ sơ']] as const;return <main className="candidate-shell"><header className="candidate-nav"><Link href="/candidate" className="candidate-brand"><span>日本語</span><b>JFT Practice</b></Link><nav aria-label="Điều hướng học viên">{links.map(([href,label])=><Link key={href} href={href} className={path===href?'active':''}>{label}</Link>)}</nav><UserMenu compact/></header>{children}<footer className="candidate-footer">Trình mô phỏng luyện tập JFT-Basic không chính thức · Không liên kết với Japan Foundation hoặc Prometric.</footer></main>}
