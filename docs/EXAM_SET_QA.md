# Exam-set QA

Assembly is deterministic and seeded. Release states are DRAFT → QA_PASSED → HUMAN_REVIEW → PUBLISHED. QA checks quotas, listening audio, reuse, pairwise overlap, diversity, and aggregate KnowledgeUnit coverage. Insufficient supply fails explicitly; constraints are never silently relaxed.
