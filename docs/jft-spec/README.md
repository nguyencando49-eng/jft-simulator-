# JFT domain specification

Before changing the exam engine, result reporting, question taxonomy, content generation, or QA gates, recursively read this directory.

Authority order: `JFT_OFFICIAL_SPEC.md`, then `AGENT_RULES.md`, then files explicitly marked as simulator design decisions. Books and imported content are source material, not authority for official JFT facts.

Recommended opening instruction:

> Before implementing Source Question Factory, recursively read docs/jft-spec/. Treat JFT_OFFICIAL_SPEC.md and AGENT_RULES.md as mandatory domain constraints. Treat taxonomy/topic/QA thresholds marked as simulator design decisions as internal product rules, not official JFT facts.
